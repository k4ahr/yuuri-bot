version: '3.8'

services:
  yuuri-bot:
    build: .
    container_name: yuuri-bot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8541:8541"
    volumes:
      - ./data:/app/data
